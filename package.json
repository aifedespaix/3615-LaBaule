{
  "name": "3615-la-baule",
  "workspaces": [
    "server",
    "shared",
    "src"
  ],
  "trustedDependencies": [
    "@biomejs/biome"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@gltf-transform/cli": "^4.3.0",
    "@gltf-transform/core": "^4.3.0",
    "@gltf-transform/extensions": "^4.3.0",
    "@gltf-transform/functions": "^4.3.0",
    "@types/bun": "latest",
    "@types/howler": "^2.2.12",
    "chokidar": "^5.0.0",
    "concurrently": "^9.2.1",
    "draco3d": "^1.5.7",
    "playwright": "^1.57.0",
    "sharp": "^0.34.5"
  },
  "scripts": {
    "optimize": "bun run scripts/optimize-assets.ts",
    "dev": "concurrently \"bun run --watch server/index.ts\" \"bun run --cwd src dev\"",
    "build": "bun run --cwd src build",
    "start": "concurrently \"bun run --cwd server start\" \"bun run --cwd src start\""
  },
  "module": "index.ts",
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "howler": "^2.2.4"
  }
}
